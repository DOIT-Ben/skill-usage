# Generated by agents-team
name: 团队目标
description: 创建需要实现与独立验收的标准 Goal
title: "[Goal] "
labels: ["status:ready", "risk:L2"]
body:
  - type: textarea
    id: goal
    attributes:
      label: Goal
      description: 用一句话定义最终必须达到的可观察结果。
    validations:
      required: true
  - type: textarea
    id: must_complete
    attributes:
      label: 必须完成
      description: 所有条目完成后才允许进入 QA，请使用任务清单。
    validations:
      required: true
  - type: textarea
    id: acceptance_gates
    attributes:
      label: 验收门禁
      description: 写明必须通过的测试命令、行为路径和证据。
    validations:
      required: true
  - type: textarea
    id: boundaries
    attributes:
      label: 任务边界
      description: 明确严禁修改、重构、读取、调用或扩展的内容。
    validations:
      required: true
  - type: dropdown
    id: risk
    attributes:
      label: 风险等级
      options: ["L1", "L2", "L3"]
    validations:
      required: true
  - type: textarea
    id: dependencies
    attributes:
      label: 依赖与阻塞条件
      description: 写明环境、权限、数据和外部服务条件；没有外部依赖时说明可在何种本地条件下执行。
    validations:
      required: true
  - type: textarea
    id: failure_rollback
    attributes:
      label: 失败处理与回滚
      description: 写明失败证据如何保留、何时停止以及如何恢复到安全状态。
    validations:
      required: true
